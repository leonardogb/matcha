<!DOCTYPE html>
<html>
<head>
    <% include ../partials/head %>
</head>

<body>
    <header>
        <% include ../partials/header %>
        <link rel="stylesheet" type="text/css" href="/css/card.css" />
    </header>
    <main>
        <div class="container">
            <div class="row">
                <h2>Profils !</h2>
            </div>
            <div class="row">
                <form method="post" action="">
                    <div class="form-group">
                        <span>Âge entre </span>
                        <input type="number" name="age-min" id="age-min" value="18" min="18" max="99">
                        <span> et </span>
                        <input type="number" name="age-max" id="age-max" value="99" min="18" max="99">
                    </div>
                    <div class="form-group">
                        <span>Popularité entre </span>
                        <input type="number" name="popul-min" value="0" min="0">
                        <span> et </span>
                        <input type="number" name="popul-max">
                    </div>
                        
                        <input type="submit" data-inline="true" class="btn btn-success" value="Submit">
                </form>
            </div>
            <div class="row">
                <% if (profil) { profil.forEach(function(element){ %>
                <a href="/profile/user/<%= element[1].login %>">
                    <div class="card">
                        <div class="box">
                            <div class="img">
                                <img src="<%= element[1].img0 %>">
                            </div>
                            <h2><span><%= element[1].prenom + " " + element[1].nom %></span><br></h2>
                            <p> <strong>Genre: </strong> <%= element[1].genre %></p>
                            <p> <strong>Âge: </strong> <%= element[1].age %></p>
                            <p> <strong>Orientation: </strong> <%= element[1].orientation %></p>
                            <!-- <p> <strong>Match: </strong> <%= element[0] %></p> -->
                            <div class="progress progress-striped">
                                <div class="progress-bar progress-bar-success" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100" style="width: <%=element[0]%>%">
                                    <span class="textoCentro"><%=element[0]%>%</span>
                                </div>
                            </div>
                        </div>
                    </div>
                <% });} %>
                </a>
            </div>
        </div>
        <div class="blockFixe">
            <div class="row contenedorChat"></div>
        </div>
        <script src="/js/socketClient.js"></script>
    </main>
    <footer>

    </footer>

</body>
</html>